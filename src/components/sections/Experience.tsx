import Carousel from "../ui/Carousel";

export default function Experience() {
    return (
        <section id="experience" className="animate-fade-up w-full pt-12 pb-12 relative" style={{ animationDelay: "0.8s" }}>
            <Carousel />
        </section>
    );
}
